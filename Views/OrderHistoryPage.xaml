<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FieldSalesForce.Views.OrderHistoryPage"
             Title="Historial">
    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">
            <Label Text="Historial de Pedidos" FontAttributes="Bold" FontSize="24" />
            <CollectionView ItemsSource="{Binding Orders}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="12" Margin="0,4" CornerRadius="12" BorderColor="LightGray">
                            <VerticalStackLayout Spacing="6">
                                <Label Text="{Binding CreatedAt, StringFormat='Fecha: {0:dd/MM/yyyy HH:mm}'}" />
                                <Label Text="{Binding TotalAmount, StringFormat='Total: {0:C}'}" />
                                <Label Text="{Binding Status}" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
